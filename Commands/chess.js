module.exports.info = {
  name: 'chess',
  regex: /ch(ess)?/,
  args: '[user]',
  desc: 'Chess',
};

module.exports.run = (message, args) => {
  const gameField = Array(64);
  //message.channel.send('ахах наебал');
};
